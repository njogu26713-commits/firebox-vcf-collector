[build]
buildCommand = "NODE_ENV=development pnpm install && NODE_ENV=production BASE_PATH=/ pnpm --filter @workspace/firebox-dashboard run build && pnpm --filter @workspace/api-server run build"

[deploy]
startCommand = "NODE_ENV=production pnpm --filter @workspace/api-server run start"
